global:
  slack_api_url: 'https://hooks.slack.com/services/'
route:
  receiver: 'slack-notifications'
receivers:
  - name: 'slack-notifications'
    slack_configs:
      - channel: '#'
        send_resolved: true
